#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    long a, b, sum = 0;
    scanf("%ld %ld", &a, &b);
    sum  = a*b;
    printf("%ld", sum);
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    return 0;
}